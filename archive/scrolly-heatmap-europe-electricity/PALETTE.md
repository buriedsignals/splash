---
ground: "#FFFFFF"
accent: "#0B7A75"
origin: newsroom
---

The answer recorded for this beat: the newsroom's own house colours, as they stand in
`skills/splash/assets/root-template/NEWSROOM.example.md` (`brandColor: "#0B7A75"`,
`ground: "#FFFFFF"`). `origin: newsroom` says who chose them.

`palette`'s subject-fit branch fires here — nine sources of electricity are exactly the substances
`matchConvention` holds grounded conventions for. It was not taken, and this family gives the
sharpest reason yet: **on a heatmap the hue is the SCALE.** Every cell in the grid, whatever source
it sits under, is one share of one country's own generation, measured on one ramp. Colouring a column
by its substance would put nine ramps on one grid and destroy the only comparison the form makes.

So the fill is a stepped ramp between the direction's own ground and its own accent, per correction
28, and it satisfies `a-sequential-grid-is-one-hue-cluster` and `the-ramp-is-monotone-in-lightness`
by construction rather than by luck. The families — renewable, nuclear, fossil — are carried by the
COLUMN ORDER and by a named boundary, not by colour.

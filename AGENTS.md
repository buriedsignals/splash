# Buried Signals splash site

This is a static GitHub Pages site. The deployable pages are `index.html` and
`demos/index.html`; `CNAME` and `.nojekyll` are publishing configuration.

Keep changes static and browser-compatible. Avoid adding build tooling unless
the site structure changes enough to justify it, and verify edited pages by
opening them locally or inspecting the generated HTML.

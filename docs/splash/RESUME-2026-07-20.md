# Reprise de session — 2026-07-20 (lis-moi d'abord)

État à la fin d'une très longue session. Sert de point de reprise après compactage.

## 1. Ce qui a été LIVRÉ et mergé (produit) — `splash` `main` @ `57915d3` == `origin/rd-dev`

Tout gate-20/20, poussé sur **`rd-dev`** (fast-forward, non-force).

- **Cloudflare Pages embed adapter** — remplace fly.io ; pure-fetch (pas de wrangler, pas de Node 22) ; protocole reconstruit de la source wrangler ; prouvé e2e via harness. `skills/splash/src/cloudflare-pages.ts`, `deploy-embed.mjs` réécrit, installeur câblé + vérif permission Pages. Spec : `docs/superpowers/specs/2026-07-19-cloudflare-pages-embed-adapter-design.md`.
- **DW hauteur** (`fix/dw-chart`) — un `d3-bars` à peu de barres héritait de la hauteur DW par défaut (~800) → vide vertical. `rowDrivenDeliveredHeight` (export-aspect.ts) pinne une hauteur row-count, erre TALL (jamais de crop), ne s'active que sous le défaut. Render-vérifié : iran 2 barres 1200×800 → 1200×540.
- **Flow-decision manifest levier 1 + 1b** — `skills/splash/src/flow-decisions.ts` (registre), `save-decision.mjs` (journal sanctionné, vérif à l'écriture), gate dans `produce-all.mjs`. 3 décisions : `suggest-chart-invoked` (artefact candidates.json), `source-fidelity` (article), `producer-escalation` (transcript). + only-scoping (`applies` par décision) + producer-escalation dw-reachable (7 types) + M3 (source-fidelity host-canonique) + **auto-record spine** (`suggest-chart-invoked` auto-confirmé par l'épine). Spec : `docs/superpowers/specs/2026-07-19-flow-decision-manifest-and-ledger-loop-design.md`.
- **SKILL.md show-before-ask durci** (Gate 3b) — « SURFACE the render, THEN ask ; décrire ≠ montrer ». Adresse le récurrent « tu ne me l'as pas montré ».
- **tom-feedback-spec** mergé (10 docs .md, additifs, zéro code).

### Harness QA — `splash-harness` `master` @ `aeac9b8` (LOCAL, pas poussé)
5 checks + lever 3, tous testés (98/98) :
- `check:render-shown-before-validation` (mord sur le vrai transcript manuel + un run co2 e2e)
- `check:placement-told-at-delivery`
- `check:format-question-own-turn` (lever 2 illegal-turn)
- `check:producer-escalation-no-reason` (mord sur dumbbell, le juge est d'accord)
- **Lever 3** (boucle ledger) : `classKey` sur chaque Finding + `resolved-classes.jsonl` (pin obligatoire) + `flagRegressions` (régression bruyante), câblé dans run-e2e. Registre VIDE au départ (se peuple quand une classe est vraiment corrigée).

## 2. CONCLUSIONS honnêtes (à ne pas re-litiger)

- **La bascule `required:true` N'EST PAS réalisable pour les décisions actuelles.** Mur structurel documenté (`docs/superpowers/specs/2026-07-20-flow-decision-required-flip-design.md`, section « Update 2026-07-20 »). Seule `suggest-chart-invoked` a une preuve structurelle à l'épine (provenance) mais elle est redondante ; `source-fidelity` (article pas garanti à l'épine) et `producer-escalation` (raison conversationnelle) ne le sont pas. **NE PAS flip.** Prochain pas éventuel = décisions à preuve structurelle, pas forcer le set actuel.
- **Lever 2 : migration CADRAGE-gates-en-décisions délibérément SKIPPÉE** (redondante avec l'enforcement existant confirmedTakeaway/channel). Seul le check illegal-turn a été fait.

## 3. État git / branches (nettoyé)

- Worktrees : `/splash` (sur `feat/splash-apertus-sovereign` — dossier principal), `splash-merge` (sur `main` — où le travail a eu lieu). **36 branches résiduelles supprimées.**
- **`main` (=`rd-dev`) = MA ligne.** **`origin/main` = la ligne de TOM, DIVERGÉE** — Tom y a poussé son skill map-explainer + une landing scrollytelling. `main` a des commits que `origin/main` n'a pas et inversement. **Non réconcilié — décision en attente.**
- 2 branches à travail unique restantes : `feat/splash-apertus-sovereign` (22 commits, Apertus/Flue sovereign, additif, décision merge/garder) · `feat/atelier-e2e-test-harness` (2 docs obsolètes).

## 4. ★ CHANTIER EN COURS — fixed-plate pour map-native STORY vidéo (investigation FAITE, prête à exécuter)

Tom a amélioré son skill vidéo **`skills/map-animation/map-explainer`** (sur `origin/main`). Sa **technique de render-stability** s'applique directement à notre map-native story vidéo.

**La technique (fixed map plate)** — réf : `git show origin/main:skills/map-animation/map-explainer/references/render-stability.md` :
- Problème : `map.jumpTo()` PAR FRAME sur mouvement caméra → shimmer/jitter du basemap en capture headless (rééchantillonnage renderer).
- Fix : caméra du renderer FIGÉE (canvas rendu 1× au zoom max, conteneur ~3×), mouvement via CSS `translate()+scale()` par frame, **même transform sur les labels HTML projetés**. Données GeoJSON/paint animées impérativement, seule la caméra gelée.

**Confirmé chez nous (les 3 conditions du problème) :**
1. `map-native` story fait un tour caméra CONTINU — `cameraForFrame` (`skills/map-native/src/story-timeline.ts:57`) interpole centre/zoom sur `moveFrames` entre beats.
2. Exécuté via `map.jumpTo()` par frame (`skills/map-native/src/core/frame-ready.ts`).
3. Même moteur `@maptiler/sdk` que Tom → recette directement portable.

**Où appliquer :** Story vidéo (Symbol/Choropleth/HexGrid/Cartogram/DotDensity Story) = cible n°1. Reveal statique = jumpTo OK (inutile). Scrolly interactif = HORS sujet (WebGL live, pas de capture headless). Complémentaire de l'anti-hang existant (`continueWhenMapSettles`/`shortWayLongitudeExtent` = hang ; fixed-plate = jitter).

**Plan d'intégration :** helper partagé `map-native/src/core/` (comme `continueWhenMapSettles`) implémentant le fixed-plate ; adapter la projection overlay (labels/callouts) à la même transform ; threader dans les ~5 Story renderers au lieu du jumpTo caméra. Pas besoin de merger le skill de Tom (recette documentée).

**PREMIER PAS (avant de construire) :** PROUVER le jitter au rendu — un MP4 réel d'un Story actuel à fort pan (ex. SymbolStory) pour confirmer que le problème existe chez nous. Pas de fix sans preuve du problème. Vérif finale : MP4 avant/après, labels qui suivent.

Chantier propre pour un cycle brainstorm→plan→execute, render-vérifié.

## 5. Décisions en attente (pour toi)
1. Réconcilier `origin/main` (Tom) ↔ `rd-dev` (moi) — vraie décision d'intégration.
2. `feat/splash-apertus-sovereign` — merger dans rd-dev (additif) ou garder à part.
3. `feat/atelier-e2e-test-harness` — force-delete (`-D`) si obsolète.
4. Pousser `splash-harness` `master` sur son remote (local pour l'instant).
5. Lancer le chantier fixed-plate (§4).

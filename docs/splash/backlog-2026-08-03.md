# Backlog Splash — 2026-08-03

Registre unique de ce qui reste à faire, écrit après la session du 2026-08-02/03.
Le cap produit qui gouverne cet ordre est dans `CLAUDE.md` § « CAP PRODUIT — décidé par Rémy le
2026-08-03 ». **Ce fichier est suivi par git** — les ledgers `.superpowers` ne le sont pas.

Convention : `[ ]` à faire · `[~]` en cours · `[x]` fait · **(D)** = décision de Rémy, ne pas rouvrir.

---

## 0. Mesuré en direct le 2026-08-03 — à garder sous les yeux

- **Goose ne trouvait AUCUN skill Splash** sur la machine de Rémy (`goose skills list` → le seul
  builtin). Cause : les 9 liens de `~/.agents/skills` pointaient vers `…/Professional/atelier/…`,
  disparu au renommage. **Réparé à la main pendant la session** ; le correctif durable est la tâche 2
  du plan `2026-08-03-goose-desktop-runtime.md`.
- Après réparation : **12 répertoires liés mais 11 skills découverts** (le 12ᵉ, `image-native`, n'a
  pas de `SKILL.md` — **E9, fermé le 2026-08-04**), plus **2 parasites** (`playwright-cli`,
  `playwright-trace`) venus de `dw-chart/node_modules` **par un symlink** (E10/B6).
  ✅ **Écart refermé des DEUX côtés le 2026-08-04** : `image-native` a désormais son `SKILL.md`
  (décision de Rémy — « comme les autres moteurs »), et la mesure en `HOME` isolé donne
  **12 liés / 12 découverts**. Les 2 parasites restent (E10/B6).
- **Poids réel de la prose, mesuré par Goose lui-même** : `splash` **33 389** tokens de contenu ·
  `suggest-chart` 12 411 · `map-native` 9 438 · `chart-native` 5 622.
  ⚠️ **Ce chiffre ne compte QUE la prose, et c'est la moitié la moins chère.** Re-mesuré le
  2026-08-04 (E10) : l'énumération du répertoire coûte **plus** que la prose, la chaîne
  `splash` → `suggest-chart` fait **90 366** tokens, et `load_skill(splash)` **déborde** le seuil de
  Goose — `SKILL.md` n'entre alors jamais dans le contexte. Voir E10.

---

## A. Préparer la publication (le dépôt reste PRIVÉ pour l'instant) **(D)**

Rémy : « on garde private mais à la fin il sera rendu public donc il faut préparer l'ensemble pour que
ça fonctionne le jour J. »

- [ ] **A1 — `REPO_URL` prêt à basculer.** `docs/installer/generate.js` porte un TODO
      « confirm before public release » ; `bun run release:check` échoue tant qu'il est là. Préparer
      la valeur définitive **sans** publier.
- [ ] **A2 — Scrub des trailers.** `scripts/scrub-trailers.sh --yes`, **destructif** (réécrit tous les
      hashes), à faire **une seule fois juste avant** le push public. 32 commits au dernier comptage.
- [ ] **A3 — Recadrer les promesses publiques** sur ce qui est vérifié (`docs/RELEASE.md:21`). L'item
      dit « envisager un lanceur `.command` » : **périmé, il existe** (`bootstrap.sh:97-105`).
      Réécrire en disant ce qui est vrai : aucune commande à taper, un terminal à l'installation.
- [ ] **A4 — Vérifier qu'aucune attribution vendor ne subsiste** dans les artefacts publiés.
- [ ] **A5 — Répétition générale.** Cloner le dépôt comme un inconnu (sans `.env`, sans clés) et
      dérouler l'install complète. C'est le seul test qui prouve le « jour J ».

## B. Les apps de bureau, pas le terminal **(D)**

Ordre imposé : **Goose d'abord**, puis Claude Desktop, Gemini desktop, ChatGPT desktop.
**Le découpage de `SKILL.md` passe au second plan** — tester d'abord, voir où ça bloque.

> Les quatre cibles ne sont pas de même nature (§ CAP PRODUIT dans `CLAUDE.md`) :
> Goose Desktop et Claude Desktop lisent des skills au format ouvert **en local** et exécutent —
> pour Claude Desktop la porte est `~/.claude/skills/`, mesurée le 2026-08-04 (B2) ;
> Gemini n'implémente le standard que dans sa **CLI** (confirmé, B3).
> ⚠️ **La phrase qui suivait sur ChatGPT a été MESURÉE FAUSSE le 2026-08-04** (B4) : elle disait que
> le modèle connecteur « interdit *a priori* de lancer Bun/Playwright/Remotion chez le journaliste ».
> La doc OpenAI documente les serveurs MCP **STDIO lancés en process local**. L'exécution locale
> n'est pas le blocage ; ce qui manque est la **surface** (MCP expose des outils, pas des skills).

- [~] **B1 — Goose.** Plan : `docs/superpowers/plans/2026-08-03-goose-desktop-runtime.md` (7 tâches).
      **Tâches 1 à 5 FAITES** (app installée par Rémy le 2026-08-03) : le gate est fermé — l'app
      découvre, suit les symlinks, **exécute**, et **atteint `bun`** via le PATH d'un shell de login
      qu'elle récupère elle-même (`docs/installer/goose-desktop-findings.md`) ; la tâche 2 était déjà
      fusionnée ; le module, son entrée de setup et la **preuve niveau A** sont livrés
      (`docs/installer/goose-desktop-proof.md`), **et le lanceur du Dock corrigé** — `open` perd le
      `cd` du `.command`, donc l'app s'ouvrait dans `$HOME`, le seul dossier où aucune commande de
      notre prose ne résout.
      **★ NIVEAU B ATTEINT le 2026-08-03** — un **vrai PNG 1200×676** produit depuis l'article d'un
      journaliste, via `dw-chart` : titre = takeaway confirmé, source tracée, densité `article-web`.
      Trajectoire : `load_skill(splash) → suggest-article → suggest-chart ×2 + delegate`, **101
      messages, 32 commandes shell** — l'invocation imbriquée par leur nom, sur un hôte non-Claude-Code.
      Provider : **palier gratuit OpenRouter**, `nemotron-3-ultra-550b:free` (1 M de contexte ; 20
      req/min, 50/jour sans achat). Le palier gratuit Gemini, lui, plafonne à **20 requêtes/jour et
      par modèle** — insuffisant, c'est ce qui avait fait échouer les trois premières tentatives.
      **Le critère 6 de l'audit est CONFIRMÉ EN DIRECT** : `publicUrl
      https://datawrapper.dwcdn.net/A74QR/1/` existait **au produce**, avant relecture — le constat le
      plus grave de l'audit, que celui-ci disait n'avoir jamais observé.
      **`verified` reste `false`** : 2 critères sur 6 échouaient. **Les deux causes sont fermées**
      dans le produit — E12 (`produce-all` écrit son rapport) et le reçu `_shown` (`lib/host/cli.ts
      present` le produit ; il manquait dans l'étape 3b, il n'était que dans la liste des Never).
      Chaîne re-vérifiée de bout en bout sur le graphique du run : sans `present` le gate refuse,
      avec lui « render approved ». **Le flag ne bascule pas pour autant** — corriger la cause ne
      vaut pas avoir revu l'effet : il faut **un run hôte de plus où les six critères sont observés**.
      Deux tentatives le même jour, arrêtées par des limites de palier gratuit et non par le produit
      (nemotron refuse de reprendre une session contenant un `read_image` ; `gemma-4-31b-it:free`
      rate-limité 10 fois d'affilée). **Pour le re-run** : un modèle gratuit acceptant l'image
      (`gemma-4-31b-it:free`, `nemotron-3-nano-omni:free`) sur la session `splash-lb2`.
      Tâche 7 faite : gate **21/22**, seul l'ambiant `lib/brain/eligibility`.
      ★ **RE-RUN du 2026-08-04 : `verified` reste `false`, pour une TROISIÈME raison, et c'est la
      plus instructive.** Sur `main` @ `88c334fb` (donc AVEC les deux correctifs dus), provider
      OpenRouter `nemotron-3-ultra-550b:free`, session `splash-lb3` : critères 1 et 6 re-observés
      (invocation imbriquée par nom ; URL Datawrapper publique `KA0XO` dès le produce), un vrai PNG
      de 66 Ko produit — mais **`produce-all.mjs` n'a JAMAIS été lancé**. Pas « lancé sans la
      redirection » comme le 2026-08-03 : jamais lancé. Le modèle a écrit son propre producteur
      (`/tmp/produce-chart.ts`, `import { produceChart } from ".../dw-chart/src/produce"`, spec à la
      main). Vérifié au registre de session, pas déduit. Donc pas de `report.json`, pas de reçu
      `_shown/`, pas de gate de rendu — **non pas parce qu'un gate a été sauté, mais parce que la
      colonne vertébrale qui les héberge a été contournée**. Conséquences : E12 est nécessaire et
      **pas suffisant** ; **la limite qu'E11 avait écrite est observée une deuxième fois** (un
      contrôle sur la colonne ne se déclenche que si la colonne tourne) ; et l'improvisation **n'est
      pas un artefact de petit modèle** — 550B a préféré écrire un producteur plutôt qu'appeler le
      nôtre. Arrêt sur `free-models-per-day` (50/jour). Détail : `goose-desktop-proof.md`.
- [x] **B2 — FERMÉ le 2026-08-04 : oui, un tiers peut déposer un skill — la porte est `~/.claude/skills/`.**
      Mesuré dans le bundle 1.12603.1 : l'app auto-charge tout répertoire de skill sous
      `~/.claude/skills/` (marketplace réservé `@skills-dir` + sentinelle de réglages managés), et
      **monte ce même répertoire en lecture seule dans la VM Cowork**. Le répertoire
      `…/local-agent-mode-sessions/skills-plugin/…` **n'est PAS la porte** (`plugin.json` =
      « anthropic-skills », `creatorType: "anthropic"`, migration marketplace distante). Et
      `~/.agents/skills` — la seule porte que remplissait notre helper — **n'est pas lue par cette
      app** : un module qui appelait le helper nu aurait installé un runtime qui ne découvre rien,
      en silence. **Livré** : `install/runtimes/claude-desktop.sh`, `link_agents_skills` prend une
      cible (un helper, mêmes règles, plusieurs portes), runtime `claude-desktop` enregistré en
      `verified: false`. Limite mesurée : `open -a Claude <dossier>` ne laisse aucune trace
      observable (contrairement à Goose) → la racine se choisit dans l'app. Détail :
      `docs/installer/claude-desktop-findings.md`.
- [x] **B3 — FERMÉ le 2026-08-04 : pas de module.** Le standard Agent Skills est documenté pour la
      **CLI seule** (`geminicli.com/docs/cli/skills` : `~/.gemini/skills/` ou l'alias
      `~/.agents/skills/`, toutes les commandes sont `gemini …`). Rien n'y mentionne l'app
      desktop/macOS lisant un `SKILL.md` — l'accès aux fichiers locaux de Spark n'est pas de la
      découverte de skills. Au passage, ça **confirme** la revendication de notre `gemini.sh`.
      Re-qualifier si Google documente les skills pour l'app.
- [x] **B4 — FERMÉ le 2026-08-04, et LA PRÉMISSE ÉTAIT FAUSSE.** La doc OpenAI
      (`learn.chatgpt.com/docs/extend/mcp`) dit que l'app ChatGPT supporte les serveurs MCP
      **STDIO — « servers that run as a local process (started by a command) »** (champ `command`),
      et que « the ChatGPT desktop app, Codex CLI, and IDE extension support MCP servers and share
      MCP configuration » via `~/.codex/config.toml`. Donc **l'exécution locale n'est pas le
      blocage** — la raison d'arrêter les frais a disparu. Ce qui manque n'est pas un adaptateur :
      c'est la **surface** (MCP expose des *outils*, pas des skills ; rien n'indique que cette app
      lise un `SKILL.md`). L'atteindre = publier Splash en serveur MCP, **une décision de
      distribution, pas un module**. ⚠️ Hypothèse moins chère à tester d'abord : la surface est
      **Codex embarqué**, et `codex` est un runtime qu'on livre déjà `verified: true` — si ce Codex
      lit `~/.agents/skills` comme la CLI, l'app est peut-être déjà atteignable sans une ligne de
      code. **Non testé : l'app n'est pas installée sur cette machine.**
- [x] **B5 — FAIT le 2026-08-03** : la couche B est atteinte sur Goose Desktop (vrai PNG, article
      réel, `dw-chart` statique). Ce qui reste n'est pas « un hôte non-Claude va-t-il jusqu'au
      fichier » — c'est le re-run 6/6 de B1 ci-dessus.
- [ ] **B6 — Les 2 skills parasites** (`playwright-cli`, `playwright-trace`) que Goose remonte depuis
      `dw-chart/node_modules`. **Cause établie le 2026-08-04 : ils entrent par un SYMLINK** — `find`
      ne le suit pas, l'hôte si. C'est le même mécanisme qu'E10, donc **B6 se ferme avec E10**, pas à
      part. ⚠️ La piste « `.gooseignore` » notée ici est **morte** : ce fichier n'existe pas en
      Goose v1.45.0 (grep de tout l'arbre à ce tag).

## C. Ce qui est construit doit fonctionner ET être atteignable **(D)**

Rémy : « il faut que ce qu'on a produit en feature fonctionne et soit accessible pour produire ».

- [ ] **C1 — Audit d'atteignabilité (prioritaire, avant le lot route).** Pour **chaque** capacité que
      le moteur sait calculer, établir si elle atteint réellement le lecteur. La session du 2026-08-02
      a trouvé **deux** cas par hasard : le storyboard de route qui ne sort nulle part, et le scrolly
      livré qui jetait les phrases confirmées de 4 types sur 6. Un balayage systématique donnerait la
      liste complète au lieu de la découvrir au compte-gouttes.
- [ ] **C2 — Le lot route** (sortira de C1, déjà cadré) : il manque **deux** composants —
      `ScrollyRouteMap` (la piste scrolly web n'a que six composants carte) et `RouteStory` (le mode
      vidéo à étapes ; la route n'a que `RouteReveal`, continu, qui ignore les beats **par
      conception**). Plus : brancher ou supprimer `RouteScrolly.tsx`, qui existe, marche, et n'est
      enregistré nulle part — c'est lui qui a fait croire à une revue que la capacité existait.
      `resolveRouteWalk` fournit déjà territoire + caméra + texte par beat : les deux composants
      consomment le même trajet résolu.
- [~] **C3 — Intégration CMS : proposer l'insertion DIRECTE dans l'article.**
      **We.Publish FERMÉ le 2026-08-04** — détail + mesures : `docs/splash/cms-insertion-2026-08-04.md`.
      Deux trous empilés, pas un : (a) `embed-cms` était implémenté et annoncé à l'INPUT depuis le
      2026-07-27 mais **inatteignable** — le menu de l'EXPORT n'offrait que a/b/c ; (b) ce qui
      existait écrivait un **article porteur**, pas l'article du journaliste.
      Livré : **forme (d)** dans la proposition (`export-code.mjs`, offerte seulement si la route
      CMS est configurée, `needsArticle: true`, refus du placeholder `<slug>` aux DEUX portes) +
      `publish-cms.mjs` (mêmes gates éditoriaux que l'embed) + **mode insertion** de l'adaptateur.
      ⚠️ **Le fait qui décide de tout** : `updateArticle` est **total** (mesuré dans la source
      amont) — insérer un bloc = **réécrire l'article entier**, donc tout champ mal relu est un
      champ RÉINITIALISÉ sur un document vivant. D'où l'aller-retour total, une table de 7 types de
      blocs **mesurée**, et un **refus** (jamais une écriture partielle) sur tout type non mesuré.
      Splash **ne publie jamais** l'article du journaliste : le visuel va dans le BROUILLON.
      **Livingdocs : mesuré, pas construit** — il a l'opération que We.Publish n'a pas
      (`insertComponent` avec `position`, `PATCH /api/2026-07/documents/{id}/commands`), donc
      **pas de risque de destruction et un travail plus petit** que celui qu'on vient de faire.
      Trois décisions dans le document ci-dessus (faire Livingdocs ou non · étendre la table de
      blocs We.Publish selon ce que Heidi.news utilise · le placement réel via l'ancre).
      Reste ouvert ici (l'original) : We.Publish **et**
      Livingdocs. Formulation de Rémy : « pouvoir proposer de directement l'intégrer à leur article
      dans leur CMS — un petit plus qui aidera les journalistes non tech ». C'est un **geste éditorial**,
      pas un export de fichier.
      - We.Publish : les adaptateurs existent et sont fusionnés (`lib/delivery/adapters/wepublish*.ts`,
        tests e2e + réseau). Reste à en faire une **proposition dans le flux**.
      - Livingdocs : **n'existe nulle part**. Modèle de composants sans rapport avec le modèle bloc de
        We.Publish → brainstorming avant tout code.

## D. Décisions prises — ne pas rouvrir **(D)**

- [x] **D1 — Datawrapper publie à l'URL publique au moment du produce.** Comportement **conservé**.
      (Vérifié : `skills/dw-chart/src/produce.ts:184` → un vrai `POST /charts/{id}/publish`, avant
      revue et avant signature.)
- [x] **D2 — Les régions sans donnée gardent le fond de carte**, jamais de teinte ni d'entrée de
      légende. Le code le faisait déjà ; la prose mentait sur **trois** fichiers, corrigés.
- [x] **D3 — Forme d'installation : on garde le `.command`.** Pas de `.app` signée (notarisation Apple
      = coût récurrent qu'un projet MIT porte mal).

## E. Dette — « un truc clean et qui fonctionne correctement » **(D)**

- [x] **E1 — FERMÉ (fix/debt-batch-1, `8de9dc2d`) — `scrollySpecErrors` accepte encore un storyboard de route**
      (`skills/scrolly/src/manifest.ts:42-72`), sous un commentaire (`produce.mjs:53`) affirmant que
      « la CLI et la colonne refusent à l'identique ». **Troisième couche qui désaccorde.** Aucun
      chemin journaliste ne l'atteint aujourd'hui.
- [x] **E2 — FERMÉ (fix/debt-batch-1, `b105d97f`) — `skills/map-native/tests/colors.test.ts` a-t-il encore un sujet ?** Sa justification
      suppose que `NO_DATA_COLOR` est rendu ; il ne l'est pas (opacité 0). La question n'est pas le
      commentaire, c'est le test.
- [ ] **E3 — §5 à §8 de `map-native/references/interactive-map-best-practices.md` NON auditées** contre
      le code. Inconnues, **pas confirmées** — §1-§4 l'ont été et corrigées, ce qui peut laisser croire
      que tout le fichier l'a été.
- [x] **E4 — FERMÉ (fix/debt-batch-1, `e6cd9b61`) — La légende écrit `1,200–1,316CHF`**, sans espace avant l'unité (chemin choroplèthe,
      `legend-format.ts`). Observé au rendu le 2026-08-02.
- [ ] **E5 — `lib/brain/eligibility.test.ts` rouge** (« a mark can never carry an empty reason »,
      `readiness.ts:54`). Échoue **aussi en isolation** en ~120 ms. Antérieur à tout ce travail, c'est
      le seul rouge du gate (21/22).
- [ ] **E6 — La « pile A — dette fermable »** de `docs/splash/residuals.md` (§2), non rouverte lors de
      cette session.
- [x] **E8 — RETIRÉ : ce n'était pas un rouge, c'était un worktree incomplet.**
      `install/preflight/model.test.ts` (« counts the three states ») rendait `summary.ready = 0` au
      lieu de `1` tant que les dépendances des moteurs manquaient ; **une fois les six installées, il
      passe 25/25**, et le gate complet le confirme (`PASS test install`).
      ★ **La leçon est dans la façon dont je m'étais trompé, et elle affine la règle existante.** Le
      projet dit déjà : « `git stash` n'établit pas qu'un rouge est pré-existant — comparer à la base
      de fusion, ou monter un worktree de contrôle. » J'ai monté le worktree de contrôle, il a rougi
      pareil, et j'ai conclu « pré-existant ». **Mais le contrôle avait exactement le même trou
      d'environnement** : deux environnements identiquement cassés sont d'accord entre eux. Un
      contrôle ne prouve la pré-existence que s'il est **complet**, pas seulement s'il est **ancien**.
- [x] **★ E12 — FERMÉ le 2026-08-03 : le gate de rendu devenait INATTEIGNABLE si le modèle oublie une redirection shell.**
      `produce-all.mjs` **imprime** son rapport sur stdout et ne l'écrit jamais ; `SKILL.md:928-935`
      demande au modèle de rediriger (« Redirecting to `report.json` is required »). Or
      `gate-render.mjs`, `apply-signoff.mjs` et `deploy-embed.mjs` prennent tous **`<report.json>` en
      argument**. Constaté le 2026-08-03 sur le run niveau B qui a **réussi** : le modèle a lancé
      `produce-all.mjs --run-dir /tmp/splash-run` sans le `>`, le JSON est parti dans le chat, et le
      gate de rendu comme la signature sont devenus injoignables — sans que rien ne le signale. Même
      classe que E11 : un gate mécanique dans son code, mais dont **l'atteignabilité** dépend de la
      discipline du modèle.
      **CORRIGÉ côté producteur, pas côté prose** : `produce-all.mjs` écrit désormais lui-même
      `<runDir>/report.json` (à côté d'`accepted.json`, `candidates.json`, `decisions.jsonl`) en plus
      de l'imprimer ; la redirection documentée reste légale et écrit les mêmes octets. `SKILL.md`
      mis en accord — il ne fait plus porter au modèle une responsabilité qui n'est plus la sienne.
      2 tests neufs, mutation vérifiée (retirer l'écriture rougit), suite `splash` 914/0.
      ★ **Une garde retirée en cours de route, et c'est la bonne décision** : un premier jet
      comparait l'inode de `fd 1` pour éviter une double écriture quand l'appelant redirige vers ce
      même fichier. **Impossible de la faire rougir** — avec `>`, les deux écrivains émettent les
      mêmes octets au même offset, donc il n'y avait rien à attraper. Supprimée plutôt que gardée en
      décoration. `goose-desktop-proof.md`.
- [ ] **★ E11 — Un run peut « réussir » SANS Splash, et rien ne le voit.** Observé le 2026-08-03 sur
      Goose Desktop : le modèle appelle `suggest_article` **comme un outil** (`-32002: Tool not
      found` — notre prose nomme un skill mais ne dit jamais **comment un hôte en invoque un** ;
      sur Goose c'est `load_skill` ou `delegate`), puis va chercher autre chose, **rallume lui-même
      l'extension `autovisualiser`** que la config déclare pourtant `enabled: false`, dessine un
      graphique dans le chat et annonce « le visuel est prêt ». **Aucun `exports/`, aucun
      producteur, aucun gate, aucun fichier possédé.** Trois chantiers distincts : (a) dire dans la
      prose comment un hôte invoque un skill imbriqué ; (b) accepter qu'un outil concurrent est à un
      appel d'extension — le désactiver par config ne tient pas ; (c) **détecter qu'on est sorti du
      pipeline** — aujourd'hui `validate-gate.ts:648-655` ne lit que `skillsInvoked`, que le modèle
      écrit (c'est le S1 de l'audit, désormais **observé** et plus seulement raisonné). Détail :
      `docs/installer/goose-desktop-proof.md`.
- [ ] **E13 — `lib/brain/typology-drift.test.ts` « DRIFT 2 » rouge : six types atteignables sans fiche KB.**
      Manquants : `scrolly:choropleth`, `scrolly:symbol`, `scrolly:locator`, `scrolly:cartogram`,
      `scrolly:hex-grid`, `scrolly:dot-density`. **Attribué le 2026-08-04 par worktree de contrôle** :
      échoue déjà sur `158ab669` (dernier commit de la session storyboard AVANT la fusion du lot
      Goose), donc ni le lot Goose ni les trois agents E9/E10/E11 ne l'ont causé — il vient du
      chantier storyboard, qui rend ce genre narratif atteignable. C'est la bonne direction (l'audit
      d'atteignabilité listait ces 8 composants comme orphelins) ; **il manque les fiches**.
      ⚠️ Piège : le contrôle a d'abord rendu « 1 error » faute de `node_modules` dans les moteurs —
      un contrôle incomplet ne prouve rien (cf. E8).
- [ ] **★ E10 — Ce qu'un hôte charge n'est pas un skill, c'est un moteur entier — et ça DÉBORDE.**
      **Re-mesuré le 2026-08-04, et le premier constat sous-estimait le problème d'un facteur 15.**
      L'hôte **ne filtre rien** (`crates/goose/src/skills/mod.rs:456-466` : ni extension, ni taille,
      ni profondeur ; seulement `.git`/`.hg`/`.svn`), et **`.gooseignore` n'existe pas** en v1.45.0.
      Surtout : `find` ne suit pas les symlinks, **l'hôte si** — donc `node_modules` rentre par là,
      invisible à nos comptages. C'est aussi le mécanisme exact des 2 parasites de **B6**.
      | skill | fichiers vus par l'hôte | tokens d'énumération | `SKILL.md` |
      |---|---|---|---|
      | `map-native` | 20 640 | **1 342 060** | 9 438 |
      | `chart-native` | 12 191 | **737 634** | 5 622 |
      | `splash` | 748 | **42 634** | 33 693 |
      **L'énumération coûte plus cher que la prose** ; la chaîne `splash → suggest-chart` fait
      **90 366** tokens, pas les ~45 000 inscrits en § 0 ci-dessus.
      **Ce n'est pas un surcoût, c'est une panne, et elle s'est produite** : `load_skill(splash)`
      rend **292 487 caractères**, au-dessus du seuil de débordement de Goose (200 000) → la réponse
      part dans un fichier temporaire et **`SKILL.md` n'entre jamais dans le contexte**. Le modèle
      l'a écrit lui-même dans le run du 2026-08-04 (« load_skill actually returns a file listing…
      My current context is now misaligned »), quatre tours perdus.
      **DÉCISION DE DISTRIBUTION À PRENDRE (Rémy)** — options chiffrées dans
      `docs/splash/skill-payload-2026-08-04.md`. Recommandation de la mesure : séparer ce qui est
      livré à un hôte de ce qui est le moteur (`chart-native` 737 634 → **11 895**, `map-native`
      1 342 060 → **6 104**). ⚠️ **Retirer `node_modules` seul NE SUFFIT PAS** : les deux débordent
      encore, et le contributeur inattendu est **`output-proof/`**.
- [x] **E9 — FERMÉ le 2026-08-04 : `link_agents_skills` ne lie que ce qui porte un `SKILL.md`** — le seul répertoire de `skills/` dans ce
      cas, il n'en a jamais eu, aucune prose n'en référence un. `link_agents_skills` le lie quand même
      (il globe `skills/*/`), donc les hôtes listent 12 répertoires et n'en découvrent que 11, en
      silence. Deux questions distinctes : (a) `image-native` doit-il être atteignable par un
      journaliste, ou reste-t-il une bibliothèque que `suggest-image` pilote ? — **question produit** ;
      (b) le helper doit-il ne lier que ce qui porte un `SKILL.md` ? Mesuré le 2026-08-03,
      `docs/installer/goose-desktop-proof.md`.
      ★ **(a) TRANCHÉ par Rémy le 2026-08-04 : « comme les autres moteurs ».** L'argument inverse
      (« une seconde porte contournerait le gate de veto de `suggest-image` ») **ne tient pas la
      symétrie** — `chart-native` et `map-native` sont déjà découvrables ET ont `suggest-chart`
      devant eux ; le couple « moteur visible + suggesteur qui décide » est le motif du dépôt, et
      `image-native` en était la seule exception. Livré : `skills/image-native/SKILL.md` (canon
      8 sections), qui dit lui-même que la porte journaliste reste `suggest-image`. **Mesuré en
      `HOME` isolé : 12 liés / 12 découverts.** Garde neuve
      `docs/installer/skills-discoverable.test.ts` : tout répertoire de `skills/` porte un
      `SKILL.md`, et son frontmatter **PARSE** en YAML.
- [ ] **E14 — Les tests d'init d'un run ne sont pas hermétiques.** Ils lisent le
      `NEWSROOM-PROFILE.md` (+ `brand.json`) **ambiant à la racine du dépôt**, non suivis par git.
      `splash-merge` en porte un qui déclare `lang: "fr"` là où le test attend `"en"` → trois rouges
      dans `lib` qui n'ont rien à voir avec le code (`init` ×2, `a non-JS host carries a run…`).
      Le défaut mord **exactement la personne la plus susceptible de toucher ce code** — celle qui
      travaille sur la charte maison — et il accuse son travail. Établi le 2026-08-04 (vingt minutes
      passées à soupçonner une fusion parfaitement saine). Même famille que le reste : un contrôle
      dont le résultat ne dépend pas de ce qu'il prétend mesurer, ici en rougissant à tort.
- [ ] **E7 — Les worktrees fusionnés encore sur disque** : `splash-storyboard`, `splash-adm1`,
      `splash-followups`, plus la dizaine d'anciens. À supprimer quand Rémy veut.

---

## Pièges d'environnement — déjà payés, ne pas re-payer

- **Un worktree neuf n'a pas de `.env`** (non suivi) → `skills/scrolly` sort 4 fail/3 errors et **aucun
  rendu n'est possible**. Remède : `ln -s ../splash-merge/.env .env`. C'est ce qui a fait conclure à
  tort à des « échecs pré-existants » et bloqué la seule vérification qui comptait.
- **`git stash` n'établit JAMAIS qu'un rouge est pré-existant** sur une branche multi-commits :
  comparer à la **base de fusion**, ou monter un worktree de contrôle.
- **Un `: ` non quoté dans une description de frontmatter fait DISPARAÎTRE le skill.** Payé le
  2026-08-04 en écrivant `image-native/SKILL.md` : « live here: the matching » est un scalaire YAML
  invalide, l'hôte **abandonne le skill sans rien dire** (12 liés, 11 découverts — le même silence
  qu'E9, par une autre cause). Un regex `/^description:\s*\S/` passe dessus : il faut **parser**.
  C'est ce que fait `docs/installer/skills-discoverable.test.ts`. Vérifier au `goose skills list`
  en `HOME` isolé, jamais au fait que le fichier existe.
- **`git checkout -- <fichier>` n'annule PAS une mutation : il restaure vers HEAD.** Payé le
  2026-08-04. Sur un fichier qui porte du travail **non commité**, « restaurer après mutation »
  efface le travail en même temps que la mutation — et sur un fichier **non suivi**, la commande
  échoue et laisse la mutation en place. Les deux sont arrivés dans la même boucle. Le bon
  instrument est une **copie sauvegardée avant la mutation** (`cp` puis `cp` en retour), et la
  vérification reste la même qu'ailleurs : comparer le checksum après restauration à celui d'avant.
  Corollaire pratique : **commiter avant de muter**.
- **Lancer un producteur réécrit des PNG `output-proof` suivis par git.** `git checkout --` avant de
  commiter ; ne jamais les stager.
- **Un gate complet exige une machine calme** ; deux gates concurrents s'invalident.

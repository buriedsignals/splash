# the-slope-carries-direction-and-the-number-carries-magnitude

- kind: imported
- name: A slope draws no value axis: the rails are plain rules and the printed ends are the scale
- applies: the beat draws two rails
- draws: value, axis
- priority: 9
- evidence: 100-datavizproject-com-data-type-viz54
- evidence: abc-net-au-news-2018-12-13-how-life-has-changed-for-people-your-age-10
- detect: the delivered plate carries no value ticks and prints a number at both ends of every line

## The rule

Two readings, two channels, and neither asks the reader to measure against an axis: the ANGLE gives
direction, the printed ends give magnitude.

## The evidence

`100.datavizproject.com`'s viz54: *"the slope carries the direction; the printed number carries the
magnitude. A reader who only looks sees three rising lines and one much steeper than the others; a
reader who reads gets the size. **Neither is asked to measure a gap against an axis.**"*

ABC: *"there is no y axis and there are no ticks. The rails are plain rules; the scale is carried
entirely by the four printed numbers."*

## What it costs, and the record says so itself

ABC's own note: without an axis, two panels of different magnitude *"are NOT comparable to each
other, and the piece does not claim they are."* A slope that drops its axis owes the reader **every
end value it draws** — which is why this treatment writes into `value` and not only into `axis`. Drop
one end label and the scale has a hole in it.

# each-rail-is-headed-by-what-it-is

- kind: imported
- name: The two rails are named at their own ends — a head on the rail, not a tick on an axis
- applies: the beat draws two rails
- draws: axis, annot
- priority: 8
- evidence: 100-datavizproject-com-data-type-viz54
- evidence: informationisbeautiful-net-visualizations-spotify-apple-music-tidal-mu
- detect: each rail carries its own head on the delivered plate, and there is no legend

## The rule

Name each rail where its marks begin and end. Then nothing has to be carried across the picture and
there is no legend to consult.

## The evidence, and why the pair proves more than either

`100.datavizproject.com`'s viz54: *"the date is a chip on the rail, not an axis tick. The two states
are named where the marks begin and end, so no legend is needed."*

Information is Beautiful runs the same geometry between two **different variables** — revenue per
play against audience — and heads *"the two rails with what they measure rather than when"*, each
with its own units at its own end.

That second record is what makes this a rule about the RAIL rather than about dates: the head says
what the rail IS, whether that is a year or a measure. A slope between two variables is the same
geometry answering a different question, and it needs the same head.

export function renderSetupPage(nonce) {
  const languages = [
    ["en", "English"], ["de", "Deutsch"], ["fr", "Français"], ["it", "Italiano"],
    ["es", "Español"], ["pt", "Português"], ["nl", "Nederlands"], ["rm", "Rumantsch"],
  ].map(([value, label]) => `<option value="${value}">${label}</option>`).join("");
  const typefaces = [
    ["system-ui, sans-serif", "System sans"],
    ["Georgia, serif", "Classic serif"],
    ["Georgia, Arial, sans-serif", "Editorial serif + sans"],
    ["custom", "Custom installed typefaces…"],
  ].map(([value, label]) => `<option value="${value}">${label}</option>`).join("");
  return `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Splash setup</title>
<style nonce="${nonce}">
:root{font-family:Inter,system-ui,sans-serif;color:#18211b;background:#eef1ea;--ink:#18211b;--muted:#5e685f;--paper:#fff;--line:#d8ded6;--accent:#24573d;--error:#a72c32}*{box-sizing:border-box}body{margin:0;background:linear-gradient(145deg,#e8eee6 0,#f7f4ec 48rem)}main{width:min(56rem,100%);margin:auto;padding:clamp(1rem,4vw,3.5rem)}header{margin-bottom:1.5rem}.eyebrow{margin:0 0 .45rem;color:var(--accent);font-size:.78rem;font-weight:800;letter-spacing:.13em;text-transform:uppercase}h1{margin:0;font-family:Georgia,serif;font-size:clamp(2rem,6vw,3.8rem);font-weight:500;line-height:1}.lede{max-width:62ch;color:var(--muted);font-size:1.05rem;line-height:1.55}.status{position:sticky;top:.75rem;z-index:2;margin:1rem 0;padding:.8rem 1rem;border:1px solid var(--line);border-left:5px solid var(--accent);border-radius:.5rem;background:#fffffff2;box-shadow:0 5px 20px #20352914}.status.error{border-left-color:var(--error);color:#7f1d24}.setup-section{margin:1.25rem 0;padding:clamp(1rem,3vw,2rem);border:1px solid var(--line);border-radius:1rem;background:var(--paper);box-shadow:0 16px 45px #23312612}.section-number{display:inline-grid;width:2rem;height:2rem;margin-right:.55rem;place-items:center;border-radius:50%;background:var(--ink);color:#fff;font-size:.85rem}h2{margin:.1rem 0 .35rem;font-family:Georgia,serif;font-size:1.65rem;font-weight:500}.section-intro{margin:.25rem 0 1.4rem;color:var(--muted)}.credential-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(min(100%,15rem),1fr));gap:.8rem}.credential{padding:1rem;border:1px solid var(--line);border-radius:.7rem;background:#fafbf8}.credential h3{margin:0;font-size:1rem}.credential p{margin:.35rem 0;color:var(--muted);font-size:.9rem;line-height:1.4}.saved{color:var(--accent)!important;font-weight:700}label{display:block;margin:1rem 0}.label{display:block;margin-bottom:.35rem;font-weight:750}small{display:block;margin-top:.3rem;color:var(--muted);line-height:1.35}input,select,button{font:inherit}input:not([type=checkbox]):not([type=color]),select{width:100%;min-height:46px;padding:.65rem .75rem;border:1px solid #8b958d;border-radius:.45rem;background:#fff;color:inherit}.two-col,.colour-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:0 1rem}.colour-grid{grid-template-columns:repeat(3,minmax(0,1fr));padding:.3rem 0}.colour-field{margin:.6rem 0}.colour-control{display:flex;gap:.5rem}.colour-control input[type=color]{flex:0 0 48px;width:48px;height:46px;padding:.2rem;border:1px solid #8b958d;border-radius:.45rem;background:#fff}.attestation{display:flex;gap:.6rem;align-items:flex-start}.attestation input{width:1.2rem;height:1.2rem;margin:.1rem 0 0}.derive-row{display:flex;gap:.6rem;align-items:end}.derive-row label{flex:1}.button,button{min-height:46px;padding:.65rem 1rem;border:2px solid var(--ink);border-radius:.5rem;background:#fff;color:inherit;font-weight:750;cursor:pointer}.primary{background:var(--ink);color:#fff}.final{display:flex;justify-content:space-between;gap:1rem;align-items:center;margin:1.5rem 0}.final p{margin:0;color:var(--muted);font-size:.9rem}.proposal{padding:.7rem 0;color:var(--muted)}a{color:#19563a;text-underline-offset:.18em}:focus-visible{outline:3px solid #d18b00;outline-offset:3px}[hidden]{display:none!important}button:disabled{cursor:wait;opacity:.6}@media(max-width:640px){.two-col,.colour-grid{grid-template-columns:1fr}.final{align-items:stretch;flex-direction:column}.derive-row{align-items:stretch;flex-direction:column}.status{top:.25rem}}
</style></head><body><main>
<header><p class="eyebrow">Visual journalism workspace</p><h1>Splash setup</h1><p class="lede">Add service access and your newsroom’s visual identity. Nothing is sent through chat: credentials go directly to your operating system’s secure credential store and branding goes to <code>NEWSROOM.md</code>.</p></header>
<p id="summary" class="status" role="status" aria-live="polite" tabindex="-1">Opening the protected local session…</p>
<form id="setup-form">
<section class="setup-section" aria-labelledby="credentials-title"><h2 id="credentials-title"><span class="section-number">1</span>Credentials</h2><p class="section-intro">Only enter services you use. Leave a field blank to keep its saved value.</p><div id="credentials" class="credential-grid"></div><div class="two-col"><label><span class="label">Cloudflare account ID</span><input id="newsroom-cloudflareAccountId" name="cloudflareAccountId" autocomplete="off" pattern="[0-9a-fA-F]{32}"><small>Non-secret. Required only when saving a Cloudflare token.</small></label><label id="cloudflare-attestation" class="attestation" hidden><input id="cloudflare-pages-scope" type="checkbox"><span><span class="label">Cloudflare Pages access</span><small>I confirm this token has Pages edit scope for the account above.</small></span></label></div><p id="legacy" class="section-intro" hidden></p></section>
<section class="setup-section" aria-labelledby="branding-title"><h2 id="branding-title"><span class="section-number">2</span>Newsroom branding</h2><p class="section-intro">These choices become Splash’s reusable house profile. Story credits are handled with each story, not here.</p><div class="derive-row"><label><span class="label">Public newsroom URL</span><input id="newsroom-url" name="url" type="url" autocomplete="url" required><small>Splash can measure public brand declarations from this site.</small></label><button id="derive" type="button">Measure site</button></div><div id="proposal" class="proposal" aria-live="polite"></div><div class="two-col"><label><span class="label">Newsroom name</span><input id="newsroom-name" name="name" autocomplete="organization" required></label><label><span class="label">Publishing language</span><select id="newsroom-language" name="language" required><option value="">Choose a language</option>${languages}</select></label></div><label><span class="label">Typeface pairing</span><select id="typeface-preset" required>${typefaces}</select><small>Choose a system pairing, or name fonts already installed and licensed by your newsroom.</small></label><label id="custom-typefaces-wrap" hidden><span class="label">Custom installed typefaces</span><input id="newsroom-typefaces" autocomplete="off" placeholder="Newsroom Serif, Newsroom Sans"></label><fieldset><legend class="label">Colours</legend><div class="colour-grid"><div class="colour-field"><label for="newsroom-brandColor" class="label">Primary</label><div class="colour-control"><input id="brandColor-picker" type="color" value="#24573d" aria-label="Choose primary colour"><input id="newsroom-brandColor" name="brandColor" value="#24573d" pattern="#[0-9a-fA-F]{6}" required></div></div><div class="colour-field"><label for="newsroom-accents" class="label">Accent</label><div class="colour-control"><input id="accents-picker" type="color" value="#d18b00" aria-label="Choose accent colour"><input id="newsroom-accents" name="accents" value="#d18b00" pattern="#[0-9a-fA-F]{6}"></div></div><div class="colour-field"><label for="newsroom-ground" class="label">Background</label><div class="colour-control"><input id="ground-picker" type="color" value="#ffffff" aria-label="Choose background colour"><input id="newsroom-ground" name="ground" value="#ffffff" pattern="#[0-9a-fA-F]{6}" required></div></div></div></fieldset></section>
<div class="final"><p>Submit validates changed credentials, saves the profile, closes this tab where the browser allows it, and continues in Goose.</p><div><button id="close" type="button">Cancel</button> <button id="submit" class="primary" type="submit">Save setup and continue</button></div></div>
</form></main><script nonce="${nonce}">
const form=document.querySelector('#setup-form');const summary=document.querySelector('#summary');const submit=document.querySelector('#submit');const closeButton=document.querySelector('#close');let state=null;let pending=false;
function announce(message,error=false){summary.textContent=message;summary.classList.toggle('error',error);summary.focus()}
function busy(value){pending=value;submit.disabled=value;closeButton.disabled=value;form.setAttribute('aria-busy',String(value))}
async function api(path,options={}){const response=await fetch(path,{...options,headers:{'content-type':'application/json',...(options.headers||{})}});const body=await response.json();if(!response.ok)throw Object.assign(new Error(body.message||'Request refused'),{body});return body}
function safeLink(raw){try{const url=new URL(raw);return url.protocol==='https:'?url.href:null}catch{return null}}
function renderCredentials(){const root=document.querySelector('#credentials');root.replaceChildren();for(const row of state.credentials){const card=document.createElement('section');card.className='credential';const slug=row.id.toLowerCase().replaceAll('_','-');const heading=document.createElement('h3');heading.textContent=row.metadata?.name||row.id;const status=document.createElement('p');status.id=slug+'-status';status.className=row.stored?'saved':'';status.textContent=row.stored?'Saved securely':'Not saved';const purpose=document.createElement('p');purpose.id=slug+'-purpose';purpose.textContent=row.metadata?.purpose||'';card.append(heading,status,purpose);const href=safeLink(row.metadata?.acquisitionUrl);if(href){const link=document.createElement('a');link.href=href;link.target='_blank';link.rel='noopener noreferrer';link.textContent='Get credential';card.append(link)}const label=document.createElement('label');const labelText=document.createElement('span');labelText.className='label';labelText.textContent=row.stored?'Replace value':'Credential value';const input=document.createElement('input');input.type='password';input.autocomplete='off';input.spellcheck=false;input.dataset.credentialId=row.id;input.dataset.generation=String(row.generation||0);input.setAttribute('aria-describedby',slug+'-status '+slug+'-purpose');label.append(labelText,input);card.append(label);root.append(card)}document.querySelector('#cloudflare-attestation').hidden=!state.credentials.some(row=>row.id==='CLOUDFLARE_API_TOKEN')}
function selectWithCurrent(select,value){if(!value)return;const match=[...select.options].some(option=>option.value===value);if(!match){const option=new Option('Current: '+value,value);select.add(option)}select.value=value}
function renderNewsroom(){const profile=state.newsroom.profile||{};for(const name of ['name','url','cloudflareAccountId','brandColor','accents','ground']){const input=document.querySelector('#newsroom-'+name);if(profile[name])input.value=profile[name]}const language=(profile.language||String(profile.languages||'').split(',')[0]).trim();selectWithCurrent(document.querySelector('#newsroom-language'),language);const preset=document.querySelector('#typeface-preset');const known=[...preset.options].some(option=>option.value===profile.typefaces);if(profile.typefaces&&known){preset.value=profile.typefaces}else if(profile.typefaces){preset.value='custom';document.querySelector('#newsroom-typefaces').value=profile.typefaces}toggleCustom();for(const name of ['brandColor','accents','ground']){const text=document.querySelector('#newsroom-'+name);const picker=document.querySelector('#'+name+'-picker');if(/^#[0-9a-f]{6}$/i.test(text.value))picker.value=text.value}}
function toggleCustom(){const custom=document.querySelector('#typeface-preset').value==='custom';document.querySelector('#custom-typefaces-wrap').hidden=!custom;document.querySelector('#newsroom-typefaces').required=custom}
async function refresh(){state=await api('/api/status',{method:'POST',body:'{}'});renderCredentials();renderNewsroom();const legacy=document.querySelector('#legacy');if(state.legacy?.exists){legacy.hidden=false;legacy.textContent='A legacy .env was detected. It is not used for new values; existing assignments remain untouched.'}}
for(const name of ['brandColor','accents','ground']){const text=document.querySelector('#newsroom-'+name);const picker=document.querySelector('#'+name+'-picker');picker.addEventListener('input',()=>{text.value=picker.value});text.addEventListener('input',()=>{if(/^#[0-9a-f]{6}$/i.test(text.value))picker.value=text.value})}
document.querySelector('#typeface-preset').addEventListener('change',toggleCustom);
form.addEventListener('input',event=>{if(event.target instanceof HTMLElement)event.target.dataset.edited='true'});
function applyProposal(field,value){if(field==='typefaces'){const preset=document.querySelector('#typeface-preset');if(preset.dataset.edited)return;preset.value='custom';document.querySelector('#newsroom-typefaces').value=value;toggleCustom();return}if(field==='languages'||field==='language'){const select=document.querySelector('#newsroom-language');if(!select.dataset.edited)selectWithCurrent(select,String(value).split(',')[0].trim());return}const input=document.querySelector('#newsroom-'+field);if(!input||input.dataset.edited)return;input.value=field==='accents'?String(value).split(',')[0].trim():value;if(['brandColor','accents','ground'].includes(field)&&/^#[0-9a-f]{6}$/i.test(input.value))document.querySelector('#'+field+'-picker').value=input.value}
document.querySelector('#derive').addEventListener('click',async()=>{const url=document.querySelector('#newsroom-url').value;if(!url){announce('Enter the public newsroom URL first.',true);return}busy(true);announce('Measuring the public newsroom site…');try{const result=await api('/api/derive',{method:'POST',body:JSON.stringify({url})});const proposal=document.querySelector('#proposal');proposal.replaceChildren();for(const [field,row] of Object.entries(result.fields)){if(!row)continue;const item=document.createElement('p');item.textContent=field+': '+row.value+' — '+row.source;proposal.append(item);applyProposal(field,row.value)}announce('Brand proposal applied. Review it before saving.')}catch(error){announce(error.body?.message||'The site could not be measured safely; enter branding manually.',true)}finally{busy(false)}});
form.addEventListener('submit',async event=>{event.preventDefault();if(pending||!form.reportValidity())return;const account=document.querySelector('#newsroom-cloudflareAccountId').value.trim();const credentials=[...document.querySelectorAll('[data-credential-id]')].filter(input=>input.value!=='').map(input=>({id:input.dataset.credentialId,candidate:input.value,expectedGeneration:Number(input.dataset.generation),validationContext:input.dataset.credentialId==='CLOUDFLARE_API_TOKEN'?{cloudflareAccountId:account,pagesScopeAttested:document.querySelector('#cloudflare-pages-scope').checked}:{}}));const language=document.querySelector('#newsroom-language').value;const preset=document.querySelector('#typeface-preset').value;const typefaces=preset==='custom'?document.querySelector('#newsroom-typefaces').value.trim():preset;const changes={name:document.querySelector('#newsroom-name').value.trim(),url:document.querySelector('#newsroom-url').value.trim(),language,languages:language,brandColor:document.querySelector('#newsroom-brandColor').value.trim(),accents:document.querySelector('#newsroom-accents').value.trim(),ground:document.querySelector('#newsroom-ground').value.trim(),typefaces,cloudflareAccountId:account};busy(true);announce('Validating and saving setup…');try{const result=await api('/api/submit',{method:'POST',body:JSON.stringify({credentials,newsroom:{expectedRevision:state.newsroom.revision,changes,confirmReplaceDecline:state.newsroom.declined}})});for(const input of document.querySelectorAll('[data-credential-id]'))input.value='';if(!result.ok){const reasons=result.credentials.filter(row=>!row.ok).map(row=>row.name+': '+(row.reason||row.status));announce(reasons.join(' · ')||result.newsroom?.message||'Some setup values could not be saved.',true);await refresh();return}announce('Setup saved. Continuing in Goose…');await api('/api/done',{method:'POST',body:'{}'});setTimeout(()=>window.close(),500)}catch(error){for(const input of document.querySelectorAll('[data-credential-id]'))input.value='';announce(error.body?.message||'Setup could not be saved.',true)}finally{busy(false)}});
closeButton.addEventListener('click',async()=>{if(pending)return;try{await api('/api/close',{method:'POST',body:'{}'});announce('Setup closed. No unsaved values were written.')}catch(error){announce(error.body?.message||'Setup could not be closed.',true)}});
(async()=>{const capability=location.hash.slice(1);history.replaceState(null,'',location.pathname);if(!capability)throw new Error('This setup link has expired.');await api('/session',{method:'POST',body:JSON.stringify({capability})});await refresh();announce('Protected local setup is ready.')})().catch(error=>announce(error.message,true));
window.addEventListener('pagehide',()=>{if(!pending)navigator.sendBeacon('/api/close',new Blob(['{}'],{type:'application/json'}))});
</script></body></html>`;
}

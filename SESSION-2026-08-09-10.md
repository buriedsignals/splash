# Session handover — 9-10 August 2026

Written for whoever picks this up next. It assumes nothing about what you remember, because the
session that produced it will be gone. Read this, then `FEEDBACK-2026-08-10.md`, then `PLAN-2026-08-10.md`.
Everything else is detail you can reach from those three.

---

## 1. What this session was

Two halves.

**The night of 9 August** closed the nine items left by the previous session and hardened the tree:
the newsroom's colours reach a render, the guards stopped lying, 1523 → 1832 tests, 75 commits. The
branch was pushed to `buriedsignals/splash` as **`splash`** so nothing depends on a local disk.

**The day of 10 August** was the first real end-to-end run **by the owner**, followed by his review of
every delivered artifact. That produced ~60 pieces of feedback, five read-only surveys, six specs,
and the execution now in flight. **This is the important half** — everything before it was measured
by agents against artifacts; this is the first time a person tried to get work done with the tool.

---

## 2. Decisions taken, and why (do not re-litigate)

| # | Decision | Reason |
|---|---|---|
| **R1** | **Map × web uses LIVE MapTiler tiles**, native zoom and pan, constrained to the subject's area. Overturns `map-web-discipline.md:357-381`, which rejected them in writing. | *"Une carte web qu'on ne peut pas parcourir est une image."* The owner was shown the cost — the key ships in the delivered HTML, the file stops working offline, MapTiler restyles so a published map can change years later — and accepted it. The baked plate remains what static and video use, and stays as the fallback layer. |
| **R1b** | **The key never enters the repository.** Committed artifacts carry a placeholder; `deliver` substitutes at delivery; a guard asserts no tracked file holds a real key; the delivered key is a **second, domain-restricted** one. | Proof beats commit their HTML and the deliverable is an MIT release — a pushed key is scanned within minutes and survives in history. MapTiler invalidates **all** of an account's keys at 100% of its spending limit, which would blank already-published maps. An account's default key cannot be restricted, so a dedicated one is needed. |
| **R2** | **Three fixed export sizes for static and video** — landscape, portrait, square — **and a RANGE for web**, which fills whatever container the CMS gives it. Charts and maps alike, one model. | *"On adapte les charts/maps en fonction."* Web is an embed component, not a fourth size. This makes the target aspect an input the camera takes, resolving the tension with geo rule 12. |
| **R3** | **The MIT/open-source question is DEFERRED for the installer.** Build the mechanism. | *"Oublie open source MIT pour le moment, passe outre."* Deferred, **not resolved** — it returns the day the release is prepared. Recorded in `FEEDBACK` Part C. |
| **R4** | **The journalist's language follows the ARTICLE, not the newsroom config, and is confirmed with them.** | A francophone paper can publish in English. Guessing is wrong in both directions. Becomes an explicit parameter, never free text. |
| **R5** | **Two scrolly forms** (see `FEEDBACK` B8.4): one visual navigated, and several visuals each navigable. Form 2 is the general case; form 1 is its single-element instance. | The vehicle must support navigation *within* an element regardless of how many it lists. |
| **R6** | **Keep Spotlight's install shape**: one static key-free script, a local configurator on `127.0.0.1` so keys never touch the shell or its history, a `0600` config, and a generated `doctor` that delegates the last word to the product's own preflight. | The owner's instruction, and the twin already owns the right-hand side (`runPreflight`). |

---

## 3. The architecture, in the terms that decide answers

**The twin is Tom Vaillant's self-contained-skill method.** A skill stays copy-pasteable on its own,
so **helpers are duplicated, never imported across skills** — enforced by `no-cross-skill-imports.test.ts`.

This decides every "how do we make X true for 23 types" question. The answer is **never** a shared
module. It is: *the same change, made identically in each craft skill, with a WALKING parity test
proving the copies stay in step.* The owner corrected me on this mid-session when I said "engine",
which is the original Splash's shape — the word alone was proposing the wrong architecture.

**Why duplication is safe here now:** `render-still-parity.test.ts` and `video-helper-parity.test.ts`
WALK the tree and compare function by function, so a new copy is guarded the moment it lands.
`helper-parity.test.ts`'s hand-written import list is the standing counter-example — it turned the
suite red for a *correct* change, and two agents kept a dead export alive to satisfy it.

**Four invariants no work may trade away:**

1. Nothing renders in a colour, value or default nobody chose; a missing input **throws** naming what it looked for.
2. Every number a reader sees is **derived** from the beat's own frozen data, never typed.
3. A beat's inputs and outputs live in the beat's own folder.
4. **A guard that cannot go red is worse than no guard** — every guard ships with the mutation that reddens it, run in a copy outside the tree.

---

## 4. Where things stand

Suite was **1972 pass / 0 fail** at last full measure. `MATRIX.md` is generated (`bun scripts/matrix.mjs --check`).

**Landed on 10 August:** the journey (grounding no longer refuses part-to-whole takeaways; the two
gates converge on recorded scalars so `checkStoryboard` returns to one argument and divergence is
impossible by construction) · palette and credits to the **seeds** (0 of 12 → 11 of 12) · the title
at full width (one line, 17 of 23 types) · the map camera (19 bakes record what they compute; live
tiles over a fallback; the mark radius derived from the camera, which uncovered `maxBounds` cropping
**6 of 13 points** out of a beat that claimed all of them) · the seven visual mechanisms (the
crossfade guard found **37 sites, not 13**) · the export-size table and its walker · three scrolly
beats.

**In flight when the session ended** — check `git log` and the working tree first:

- the scrolly scaffold, re-scoped to B8.1 (the page must not scroll)
- two single-visual scrolly beats (B8.3), told that the scroll must DRIVE the visual
- the portrait probe (A/B/C comparison — see §6)
- **install + hosts**, the largest and most consequential

---

## 5. What remains, ranked by what it blocks

1. **Install and hosts.** Both surveys found independently: **a fresh root reports `ready: true` and
   can render exactly one genre.** The template declares 6 deps against 10 used (81 files import
   remotion, 43 puppeteer, 22 maplibre); `checkDependencies` validates only the template's own list,
   so omissions are invisible by construction; `Bun.resolveSync` falls back to Bun's **global** cache
   whatever root it is asked about. Plus **one key, two homes** — producers read `twin/.env`,
   `recordKey` writes `<root>/.env`, and both runtimes resolve symlinks before computing paths, so a
   symlink install makes a producer read the *developer's* key. **Goose Desktop — the pilot's host —
   has never been probed**, only the embedded binary. Good news measured: the `load_skill` overflow
   **does not reproduce** on the twin (38 642 chars against a 200 000 threshold; the original's
   monolith was 294 111 and was dumped) — the fifteen-skill split solved it without anyone noticing.
2. **The scrolly**, on all three of B8.1/B8.2/B8.4.
3. **Portrait and mobile** — see §6, it is a design question before it is code.
4. **The typeface.** Measured, not built: resvg and Remotion both accept a custom face, but **Remotion
   paints in it while `measureText` returns the FALLBACK's number**, and **resvg never errors on a face
   it cannot resolve** — so refusal must happen before the render. Subsetting is the feasibility
   condition (+296 KB unsubsetted against 29 KB charts; +9.6 KB subset to the glyphs actually drawn),
   and `pyftsubset` strips the OFL licence records by default. **Heidi.news uses Roboto (OFL)** — the
   pilot's own case is licensable. Path: static chart genre only, 4-6 days.
5. **The remaining export-size work** — 17 written statics and 19 video beats.
6. **flow/route × web**, the one empty matrix cell, blocked on the hoverable-line primitive.
7. **`co2-suisse`** — the project's first beat, the reference everything was written against, has
   **no `BRIEF.md`** and its still has no producing script.

---

## 6. The open design question: portrait, and mobile

The probe rendered a histogram at three sizes. **Portrait came back mechanically perfect and
editorially destroyed**: 0 clipped, 0 collisions, 84% fill — the best of the three — while the plot's
aspect went 2.35:1 → 0.54:1 and the tallest bar 4.2:1 → **18.4:1**. A right-skewed distribution reads
as one column beside nine slivers. Confirmed on a line: a one-third fall draws as a cliff.
**No measurement catches it.**

The owner: *"Le portrait ressemble à la même problématique que mobile."* He is right — they are one
question.

**MEASURED — the probe landed.** `proof/portrait-aspect-probe/` holds ten renders across three types,
`PORTRAIT-MEASUREMENTS.md` (generated) and `PORTRAIT-VERDICT.md` (what a person saw). Every arm scored
0 clipped and 0 collisions, **including the three worst-reading ones** — which is the whole point.

**The rule holds, and it is two rules that cover each other's residue.** Clamping the plot to an
aspect its type supports is what brings the shape back (proved by a control arm with the clamp and no
added words: decay reads as decay again). Giving the leftover height to editorial furniture is what
stops the frame reading as a graphic that failed to load — the same control arm left 45% of the frame
blank and looked broken.

| | plot aspect | frame share | primary mark |
|---|---|---|---|
| histogram, today | 0.54:1 | 84.2% | 18.4:1 |
| histogram, clamped + furnished | 1.1:1 | 41% | 9:1 |
| line, today | 0.41:1 | 88.3% | 34% drift drawn at **80.6°** |
| line, clamped + furnished | 0.8:1 | 44.9% | 47.7° |
| ranking, columns today | 0.62:1 | 81% | 21.9:1 |
| ranking, **transposed to bars** | 0.73:1 | 58.8% | 10.7:1 |

**Global in shape, per-type in parameters, and one family supersedes it.** Types with a NOMINAL
category axis have a twin **form**, not a twin aspect — bar/column, lollipop, dumbbell, diverging
bar. Transposed, the ranking was the only arm the prober would publish. Correctly, no transposed arm
exists for the histogram, whose x axis is continuous.

**The result nobody predicted, and it should change the spec.** A fourth arm typeset for a PHONE
rather than for an article column fills the frame with no gap at all, and its plot lands at **2.39:1
— its own native aspect — with the clamp never binding.** So a large part of the "portrait wastes
height" problem is an artefact of typesetting a story post like an article graphic. The clamp is
still needed (a short title with no annotations stretches to 0.55:1 at any type size), but **the
per-size type scale is what actually decides the layout** — which is the export-size probe's own open
question arriving here with evidence.

**Home**: the **range** belongs in the type sheets, as a sixth question beside the five they already
answer — but it is **one paragraph per sheet, not a line**: the range plus where it was measured, or
a bare number nobody can defend later. Roughly a session for the chart family, since each range must
be measured by rendering and looking. About a twelfth of the sheets need a second sentence naming a
portrait *form* instead of a range. The clamp itself is four lines duplicated per craft skill with a
walking parity test; the type scale belongs in `sizes.mjs`, in no sheet.

**One stated weakness**: the line's derived floor learned from a square render that already contained
the defect, so it is wrong. A line's constraint should come from landscape only, or be expressed as a
slope target rather than an aspect.

---

## 7. The method lessons, which cost the most to learn

- **A guard that cannot go red is worse than none.** `render-still-parity.test.ts` shipped comparing
  *signatures* instead of bodies — it took the `{` of an argument's inline type as the body's — so it
  was inert for every function with a destructured or object-typed parameter. Which is most of them.
  Found only by mutating a real `wrap` and watching it stay green.
- **A guard maintained by remembering stops covering things**, and can make people write worse code:
  `helper-parity`'s list forced two agents to keep a dead export alive.
- **The instrument lies as often as the code.** Five times this session: a scanner crying drift over
  reformatting; a script marking four verified-sound PNGs as superseded; a hover probe measuring a
  response radius without ever measuring the disc; a mutation harness reading `tail`'s exit code and
  printing GREEN over a screen of FAILs; a parity guard comparing a number to itself.
- **A sampled probe is not a continuous one.** 25 scroll positions measured 25/25 correct while the
  owner watched visuals arrive at the wrong moment.
- **Looking finds what tests cannot.** Every serious defect this session came from opening the
  artifact: 6 of 13 map points cropped, a label naming a height its column had not reached, a
  crossfade painting both halves of a handover, a portrait that destroys a distribution.
- **Report only what you have read.** Two agents retracted numbers they had stated before the output
  landed — one was right by luck and said so anyway.

---

## 8. Where everything lives

- `FEEDBACK-2026-08-10.md` — the owner's ~60 items, verbatim, with a status column. **The backlog.**
- `PLAN-2026-08-10.md` — the frame: method, invariants, chantier order, required spec shape.
- `survey/*.md` — seven read-only surveys (furniture/typeface/credits, export sizes, map camera,
  journalist journey, web genre and specifics, typeface feasibility, AI hosts, installer).
- `specs/*.md` — six executable specs.
- `MATRIX.md` — generated coverage map. `AUDIT-*.md` — the artifact audits.
- `HANDOVER.md` §12 — the previous session, still the reference for how the project works.
- Branch `splash` on `buriedsignals/splash`. Run root at `../splash-run/` with `START-HERE.md`.
- Mycroft, for the install question the owner raised: `github.com/buriedsignals/mycroft`.

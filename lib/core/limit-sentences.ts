// The refusal sentences a journalist reads, when MORE THAN ONE engine says them.
//
// SYMBOL_LABELS_INTERACTIVE lived on map-native's feature-limits and map-dw imported it across
// the engine boundary (registry E16). A sentence two engines show is not one engine's to own —
// and the reason is the project's own rule, not tidiness: the offer's declaration and any later
// refusal must show the SAME string, so the wording has exactly one definition or the two
// engines drift into telling the journalist different things about the same limit.
export const SYMBOL_LABELS_INTERACTIVE =
  "on the live interactive page the circles are read by hovering them — they carry no " +
  "always-visible name+value labels (the no-JS static fallback does)";

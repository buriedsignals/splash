---
ground: "#16191B"
accent: "#D4A853"
accents: "#5B8A8A"
origin: newsroom
---

# The palette for this story

`palette`'s proposal (`proposePalette`, `skills/palette/scripts/palette.mjs`) found no subject
convention for "le poids de chaque poste dans le budget primitif 2026" — a municipal budget is not
one of the few associations a reader already holds, so the newsroom's own house colours led the
proposal, and `noConventionReason` said so out loud.

Option 1 was `recommended`: `#D4A853` on `#16191B`, measured at **8.01:1** against the ground, clear
of the 3:1 non-text floor (WCAG 2.2 SC 1.4.11). The second house accent `#5B8A8A` measured
**4.58:1** — also passing, narrower — and is recorded in `accents` because `NEWSROOM.md` carries it.

The beat draws in ONE accent. On a diverging bar the sign is already carried by direction, so the
accent is spent on the single line that runs the other way — the provision write-back — and the six
spending lines take the furniture's own `muted`, derived from the ground rather than chosen.

No journalist was present to answer this proposal interactively in this run. Following the
unattended-run path (`skills/palette/SKILL.md`, "When nobody is there to answer"), the recommended
option is recorded exactly as proposed, with `origin: newsroom` naming where it came from.

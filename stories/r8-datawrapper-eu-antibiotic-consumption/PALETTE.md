---
ground: "#16191B"
accent: "#D4A853"
accents: "#5B8A8A"
origin: journalist
---

The answer recorded for this story. `proposePalette` found no subject convention -- antibiotic
consumption carries none a reader already holds -- so the newsroom's own colours led, and option 1
was chosen: Buried Signals's charcoal ground `#16191B` with its brand gold `#D4A853`, measured at
**8.01:1** against the ground, well clear of the 3:1 non-text floor. The second house accent
`#5B8A8A` measures 4.58:1 and is recorded beside it; this beat draws one series and does not spend
it.

`origin: journalist` rather than `newsroom` because the recommendation was read and then answered,
not accepted by default.

The ground is dark, and that is load-bearing for this beat. `planExportSurface` reads it before any
chart is created and asks Datawrapper for the export on the matching side; `assertExportedSurface`
reads the returned PNG's own pixels back and refuses one painted on the other side. It also closes
one door: preflight measured that a PUBLISHED Datawrapper embed follows the reader's own colour
scheme and defaults to light, so the `web` form of this delegated producer cannot be asked for on
this ground at all. This beat is static, which can.

// core/text — shared text-fitting helpers. Moved to the shared core
// (lib/core/text-fit.ts) — re-exported here so every chart-native component
// keeps its existing core/text import unchanged.
export * from "../../../../lib/core/text-fit";

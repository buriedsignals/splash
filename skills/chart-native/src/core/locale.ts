// core/locale — re-exports the shared implementation. See lib/core/locale.ts (the
// canonical source: this file's own body until the shared-core extraction) for the
// full rationale (explicit per-language table, why not Intl, the fr/de/it/en rows).
export * from "../../../../lib/core/locale";

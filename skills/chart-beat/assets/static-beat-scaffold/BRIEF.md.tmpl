---
format: static
type: %%TYPE%%
---

# Beat — SCAFFOLD: the claim, as the title says it (static)

**Type:** %%TYPE%%. **Medium/format:** chart / **static**. **Size:** landscape (`sizeFor`, `scripts/sizes.mjs`).

SCAFFOLD: the subject, the frozen data (`data.csv`, credited here) and the claim — every number derived from the
rows and asserted in `render-directions.mjs`, never typed.

## The choreography

SCAFFOLD: read `skills/chart-beat/references/types/%%TYPE%%.md` for this type's own rules and the worked example
it names, then write the argument the component draws — geometry first and pure, furniture derived from the
ground, one accent, direct annotation.

## Precision

SCAFFOLD: what is laid out in the reader's pixels (an honest zero for length-encoded marks unless the type sheet
says otherwise), and the list of assertions `render-directions.mjs` checks the data against.

## The owner's rules — checked before the render

- [ ] Every number the plate shows is reproducible from the beat's own frozen data, or carries a written
      `grounded-by-hand` reason.
- [ ] Every colour derives from the direction's own three; no hue is imported from a reference, a brand or a
      habit.
- [ ] ONE art direction, composed from this beat's own PALETTE.md and text — never the three filed demo
      directions, except for a catalogue proof (`--filed`).
- [ ] The alt text (`role="img" aria-label`) says what the plate shows, not that it is a chart.

## Direction

One — the composer's best candidate for this beat's own PALETTE.md and text (`render-directions.mjs`):
`renders/<id>.png` + `.svg`. `--filed` renders `creme`, `nocturne`, `rapport` instead, for a catalogue proof.

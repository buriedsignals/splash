# %%BEAT%% — brief

> SCAFFOLDED. **No code before this file is finished.** The scaffold wrote the plumbing; it wrote
> none of what is below, and it could not have. Delete this blockquote when the brief is real.

Type: `%%TYPE%%` · sheet: `skills/map-web/references/types/%%TYPE%%.md` ·
static sibling: `%%STATIC%%` · pattern: `proof/web-choropleth-europe-lowcarbon`

## The one thing this map proves

<!-- One sentence. If the still already proves it, this beat does not need the web format. -->

## The gesture — argued BEFORE any code

<!--
WHICH DECISION THE AUTHOR OTHERWISE TAKES IN SILENCE IS HANDED TO THE READER, and why THIS subject
makes that decision visible. Name it in the subject's own terms, not the vocabulary's.

Then: which vocabulary spends it (map-web/SKILL.md, "The map vocabularies"), or that this beat needs
a new one, or that it needs none. A type that deserves no gesture is a legitimate outcome and it is
better than borrowing a neighbour's. What is reused across beats is the MECHANISM; the gesture never
is.

The unfiltered/default state must already show the whole claim the title makes. A gesture that hides
argument-bearing content behind an interaction is refused whatever it is called.
-->

## The camera

<!--
The window, the units it holds whole, the one it deliberately does not, and WHY THIS PROJECTION —
measured, not asserted. Then: what Web Mercator costs this subject, as a number this page prints.
-->

## The two layers

<!-- What a reader gets with JavaScript off / offline / after a key lapses, and that it is complete. -->

## The reader who cannot see the shape

<!--
A hover tooltip is not an accessible answer to a spatial medium: it must be found first, which is
what a screen-reader user cannot do. Decide about the table, in writing, and say what leaving it off
costs. Do not choose by not deciding.
-->

## The refusals

<!-- What this beat asserts about the MARKUP, and the matching assertion about the LIVE PLAN. -->

## Verification

- [ ] `bun %%BEAT_PATH%%/render-directions-web.mjs` — three directions, none refused
- [ ] the keyed `.local.html` copy opened and DRIVEN: pan, zoom, hover, Tab, the gesture
- [ ] screenshotted at 1600 / 1024 / 768 / 375 and LOOKED AT — a computed value can lie
- [ ] JavaScript off: the fallback stands, complete

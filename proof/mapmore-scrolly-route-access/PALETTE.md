---
ground: "#16191B"
accent: "#13A99B"
origin: journalist
---

The answer recorded for this beat: the dark ground the delivered file declared, and a teal for the
route of evening stops that can be seen on it.

`origin: journalist`. The subject carries no convention a reader already holds — "evening cooling
stops" is not a category with a colour — so `matchConvention` finds nothing and the accent is the
journalist's own.

**The ground is the original's, and it is why the plate was re-baked.** The delivered file declared
`--ground: #16191B` and painted every label white on a dark halo — furniture that is right for that
ground — over a basemap baked in `dataviz-light`. The theme and the plate disagreed, and the first
rebuild here resolved it the wrong way round, by moving the theme to the plate. `bake.mjs` bakes
`dataviz-dark` at the original's own recovered camera instead, so the plate follows the theme that
was chosen rather than the other way about.

**The accent moved, and the measurement is why.** The original's #0B6B61 reads **2.77:1** against
this ground — below the 3:1 a line this thin owes a reader, and it only ever worked because the
plate underneath it was light. Same hue, lifted until it clears: #13A99B measures **6.04:1** on the
ground and **5.63:1** on the baked plate's own mean (#1F1F1F). A colour that cannot be seen on the
ground it is drawn over is not a palette answer, it is a preference.

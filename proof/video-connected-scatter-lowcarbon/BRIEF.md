---
format: video
size: landscape
type: connected-scatter
medium: chart
grounding: supported
---

# Beat — Tous plus propres chez eux, 5 plus légers en Europe (video)

**Type:** connected scatter (chart). **Medium/format:** chart / **video**. **Size:** landscape (1920 × 1080).

Same subject, same frozen file (`../static-connected-scatter-lowcarbon/data.csv`, Ember / Energy Institute via Our World in
Data) and the same derivation as `proof/static-connected-scatter-lowcarbon`: sixteen countries at 2000 and 2024, each one's
weight in the group's low-carbon electricity (x) against the low-carbon share of its own (y). Asserted: all sixteen cleaner
at home, a minority of five lighter in the group, France cleaner, producing more and lighter by more than five points, the
heaviest of 2000, and the only country with Germany outside the close-up's 0–12 %.

## The picture — shots, not a page

1. **The title card** (from frame 0, 1.5 s).
2. **The story** — the scatter across the frame; the counters and the key in one panel with no plate, seated in the
   plot's corner no mark reaches at either scale.
3. **No end card** — the video ends on the whole chart, the five picked out, France's two moves; the credit on one line.
   About 24 s.

## The choreography

What only a video can do is **make the move happen**: each country leaves its hollow 2000 ring and slides along its arc to
its 2024 disc, and the viewer sees every arc go up before any sentence says so.

| event | what the shot says | gesture | what the viewer sees move | derived value asserted |
| --- | --- | --- | --- | --- |
| `establish` | the question | — | the title card | — |
| `reference` | what the two axes are | **explain** | the axes and the key; each country's 2000 weight in the group grows as a bar from zero at the height of its own mix — France's runs almost across — then every bar collapses into its right end: the 2000 ring | France the heaviest |
| `reveal` | every one cleaned up | **trace + count** | the countries travel one after another, largest clean-up first, each arc drawn as far as its disc; « {n} sur 16 plus propres » climbs as each lands | all sixteen higher |
| `subject` | the crowd near the origin | **zoom** | the x axis closes onto 0–12 %, France and Germany leave the frame, the crowd opens and every country in it is named | only France and Germany outside |
| `conclusion` | five weigh less, France most | **pull back + filter + name + release** | the whole axis again; the five that moved left picked out one after another, largest loss first, « {n} plus légers »; the rest step back; France's arc split into « −11,8 pts » across and « +4,2 pts » up; then every country comes back — the whole chart, the five and France still marked; the credit on one line | the five; France's two moves |
| `hold` | the picture | — | nothing | hold = conclusion |

## Write as little as the picture allows

No standfirst, no reading line: the move happens on screen. The counters are the counts; the legs carry France's two
values; the names are names.

## Directions

`creme`, `nocturne`, `rapport` — `renders/<id>.mp4`, `renders/<id>-final-frame.png`, `renders/<id>-props.json`.

---
ground: "#FFFFFF"
accent: "#0B7A75"
origin: newsroom
---

The answer recorded for this beat: the newsroom's own house colours, as they stand in
`skills/splash/assets/root-template/NEWSROOM.example.md` (`brandColor: "#0B7A75"`,
`ground: "#FFFFFF"`). `origin: newsroom` says who chose them.

**The reference's twelve hues are a category this beat does not have.** Open Innovations fills each
hex by region, and its sibling by winning party — eleven named colours, which is the right answer
when the quantity IS a category. Here the quantity is a rate, so the fills are one stepped ramp
between the direction's own poles: `the-scale-is-stepped-not-continuous`, five classes, breaks
printed in the data's units, as the choropleth and the cartogram draw them.

**The cell separator is the ground's own colour, which is the reference's rule and not a
compromise.** Its style route counts 652 marks with a white stroke on a `#EFEFEF` page: the hexes are
separated by the page showing through, so the grid reads as a tiled surface rather than as scattered
marks. Here the stroke is `direction.ground`, for the same reason.

Ukraine is on the plate and is not in the measure — it is the origin of the people the other cells
count. It takes the neutral the cartogram uses for a missing reading, which is outside the ramp by
construction.

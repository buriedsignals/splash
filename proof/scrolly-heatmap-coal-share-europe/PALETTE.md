---
ground: "#FFFFFF"
accent: "#3A3A3A"
origin: subject
---

The answer recorded for this beat: `palette`'s **subject** option, as for `static-heatmap-coal-share-europe`.

`matchConvention("coal's share of electricity")` returns exactly one grounded convention — `fossil` — and returns it
alone. Near-black grey reads as coal, the material's own colour. Drawn through the filed directions, each direction
keeps its own accent; the ramp runs from a pale stop floored at 3:1 against the ground to the accent carried toward the
ink, in five classes whose breaks are the thresholds the cards read (10 % and 50 %).

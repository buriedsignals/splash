---
ground: "#FFFFFF"
accent: "#0B7A75"
origin: newsroom
---

The demonstration newsroom's own house colours, byte-for-byte the pair recorded in
`skills/chart-beat/PALETTE.md` and in `proof/static-bar-top-emitters-2024/PALETTE.md`
(`NEWSROOM.example.md`'s `brandColor` / `ground`).

This probe compares LAYOUTS, so the colours are held constant across every arm on purpose: if the
three arms differed in colour as well as in geometry, nothing the eye reported about them could be
attributed. `portrait-probe.mjs` reads this file with `readPalette` and names no hex of its own —
the same rule a beat is held to, applied to a probe so the probe cannot be dismissed for breaking it.

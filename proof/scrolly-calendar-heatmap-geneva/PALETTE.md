---
ground: "#FFFFFF"
accent: "#0B7A75"
origin: newsroom
---

The newsroom's house colours.

**One hue, six bins, and the ramp is tested rather than looked at.**
`a-sequential-grid-is-one-hue-cluster` rests on a measurement across this family: Observable draws
this form with `turbo` and this base's own pixel route classified that plate **`categorical`, with
four hue clusters**, where each of the four disciplined references reads `sequential` with one. A
ramp a clustering reads as four categories will read as four categories to a reader.

So the ramp steps the DIRECTION's own accent between two poles built from its ground and its ink —
one hue by construction, which is what `METHOD.md` correction 28 asks for anyway, and it passes the
clustering test without a test being needed.

The no-data cells take a neutral **outside the ramp** and are separated from the ramp's own low end
by LIGHTNESS as well as hue, which is Datawrapper's counter-lesson: hue alone does not survive
greyscale. The one thing drawn in the accent at full strength is the outline of the streak the
headline is about — ABC's *"one cell in a hue outside the ramp names the extreme, so it cannot be
misread as a value"*.

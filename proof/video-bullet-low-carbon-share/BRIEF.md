---
format: video
size: landscape
type: bullet
---

# Beat — Pologne : +17,3 points de bas-carbone depuis 2015, toujours la seule sous la moitié (video)

**Type:** bullet (chart). **Medium/format:** chart / **video**. **Size:** landscape (1920 × 1080).

Same subject, same frozen file (`../static-bullet-low-carbon-share/data.csv`, Ember / Energy Institute via Our World in
Data) and the same derivation as `proof/static-bullet-low-carbon-share`: the low-carbon share of six countries' electricity
in 2015 and 2024, ranked by gain. Asserted: the country that gained most is the only one still under half; the insertion
from the 2015 order ends on the order of gain.

## The picture — shots, not a page

1. **The title card** (from frame 0, 1.5 s).
2. **The story** — six tracks to 100 %, the thick pale 2015 bar and the thin saturated 2024 bar in one hue, the names at the
   left, the gains in a column at the right, the ticks under; over them the unit at the left and the key at the right.
3. **No end card** — the video ends on the rows by gain, the half line dropped, Poland alone kept; the credit.

## The choreography

What only a video can do is **re-sort in front of the viewer**: the rows start in their 2015 order — Poland last — and,
once the gains are counted, re-sort by gain one row at a time, Poland climbing past all five.

| event | what the shot says | gesture | what the viewer sees move | derived value asserted |
| --- | --- | --- | --- | --- |
| `establish` | the question | — | the title card | — |
| `reference` | six countries, a track to 100 % | — (furniture) | six empty tracks in their 2015 order, the names, the key | the 2015 order |
| `reveal` | 2015, then 2024 | **reveal in order + count** | the 2015 bars extend from zero row after row; the 2024 bars extend on from the 2015 ends, each gain counting in points | every gain |
| `subject` | Poland gained most | **reorder** | the insertion: one row at a time climbs to its slot over the rows it passes, which step down one slot together; Poland first, to the top | the order of gain |
| `conclusion` | still the only one under half | **reference line + filter** | the 50 % line drops; every row but Poland steps back; the credit | the only one under 50 % |
| `hold` | the ranking | — | nothing | hold = conclusion |

No zoom onto 90–100 %: the scrolly's fifth card proves a second sentence the title does not make.

## Write as little as the picture allows

No standfirst, no state names, no reading line: the key says which bar is which year; the gains are the counts.

## Directions

`creme`, `nocturne`, `rapport` — `renders/<id>.mp4`, `renders/<id>-final-frame.png`, `renders/<id>-props.json`.

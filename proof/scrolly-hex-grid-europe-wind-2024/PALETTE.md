---
ground: "#16191B"
accent: "#D4A853"
origin: newsroom
---

The answer recorded for this beat: the newsroom's own house colours
(`NEWSROOM.md`, `brandColor: "#D4A853"`, `ground: "#16191B"`). `origin: newsroom` says who chose
them — the subject (wind generation share) carries no convention of its own, so the newsroom's
colours lead.

The quantity is a rate (share of national electricity from wind), not a category, so the fills are
one stepped ramp between the direction's own poles, five classes, breaks printed in the data's
units — the same reasoning the hex-grid worked example (`proof/scrolly-hex-grid-europe-protection/PALETTE.md`)
records for its own rate.
